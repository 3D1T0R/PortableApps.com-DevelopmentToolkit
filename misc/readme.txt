This contains miscellaneous scripts used for checking the integrity of various
things and perhaps other scripts. They have little relevance to the
PortableApps.com Development Toolkit itself and are only intended to be run
directly, so ``misc`` is not a package and there is no ``__init__.py`` here.
