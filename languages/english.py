import languages

class English(languages.Language):
    LANGUAGE_NAME = 'English'
    LANGUAGE_NAME_NATIVE = 'English'
    # All string defaults are defined in Language as it's the default

language = English()
